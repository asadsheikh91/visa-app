# Test-only dependencies. Not installed in the production image — see Dockerfile,
# which installs requirements.txt alone.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0

# FastAPI's TestClient is a thin wrapper over httpx; starlette does not pull it in
# as a hard dependency, so the test suite has to ask for it directly.
httpx==0.28.1

# Async SQLite driver. The suite runs against an in-process database rather than a
# real Postgres, so no service container is needed to run the tests.
aiosqlite==0.22.1
