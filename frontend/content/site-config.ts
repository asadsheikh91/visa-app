// Site-wide credibility claims that need to stay trivially updatable.

/** Shown in the hero trust bar. Bump this whenever the rules/sources are re-audited.
 *  Sentence case in source — the trust bar renders it uppercase via CSS, same as
 *  the rest of that list. */
export const RULES_UPDATED_LABEL = 'Updated July 2026'
